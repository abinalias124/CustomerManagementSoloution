import { OnlyNumberDirective } from './only-number';

describe('OnlyNumber', () => {
  it('should create an instance', () => {
    const directive = OnlyNumberDirective;
    expect(directive).toBeTruthy();
  });
});
